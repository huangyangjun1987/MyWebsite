## MODIFIED Requirements

### Requirement: 主题切换按钮位置
系统 SHALL 将主题切换按钮渲染在导航栏的右侧，位于导航链接之后。

#### Scenario: 切换按钮在导航栏右侧可见
- **GIVEN** 用户访问站点
- **WHEN** 导航栏渲染完成
- **THEN** 主题切换按钮固定在导航栏右侧
- **AND** 按钮位于导航链接"联系我"之后
- **AND** 按钮可点击，不被其他元素遮挡
- **AND** 按钮与导航栏其他元素在同一水平线上对齐
